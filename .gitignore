node_modules/
.venv/
dist/
*.log
.DS_Store

# Build artifacts (keep build/ for entitlements)
out/

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# macOS
.AppleDouble
.LSOverride

# Release artifacts
*.dmg
*.zip
*.blockmap

# Test Artifacts
test_*.png
tests/screenshots/
tests/golden/*Z.png
tests/reference-pages/screenshots/
tests/validation/rosenholtz-maps/
tests/validation/results/
tests/validation/results-*/

# Golden captures — bulk regression screenshots, too large for git.
# Key illustrative captures live in docs/golden/ instead (curated per release).
tests/golden-captures/
tests/smoke-captures/

# Capture pipeline manifests (per-directory skip cache)
.capture-manifest.json

# OCR test artifacts
*.traineddata
tests/validation/ocr-accuracy-curve.json
# Research papers (PDFs, too large for git)
docs/research/

# Private correspondence
docs/meeting-prep-*.md
docs/meeting-questions-*.md
docs/transcripts/

# LaTeX build artifacts
docs/arxiv-paper/*.aux
docs/arxiv-paper/*.bbl
docs/arxiv-paper/*.blg
docs/arxiv-paper/*.out

build/certs/
build_error.txt
build_error.txt
