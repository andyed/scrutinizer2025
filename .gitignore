node_modules/
.venv/
dist/
*.log
.DS_Store

# Build artifacts (keep build/ for entitlements)
out/

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# macOS
.AppleDouble
.LSOverride

# Release artifacts
*.dmg
*.zip
*.blockmap

# Test Artifacts
tests/screenshots/
tests/golden/*Z.png
tests/validation/rosenholtz-maps/
tests/validation/results/
tests/validation/results-256/

# Golden captures — bulk regression screenshots, too large for git.
# Key illustrative captures live in docs/golden/ instead (curated per release).
tests/golden-captures/
build/certs/
build_error.txt
